#include<iostream>
#include<conio.h>
using namespace std;

int area(int,int);
float area(int);
int main()
{
    int r;
    cout<<"Enter radius of a circle";
    cin>>r;
    float A=area(r);
    cout<<"\n Area of circle is "<<A;
    int l,b,a;
    cout<<"\n Enter length and breadth of rectangle";
    cin>>l>>b;
    area(l,b);
    Cout
}